const initialState = {
  images: [],
  count: 2,
  loading: false
};

export default function imagesReducer(state = initialState, action) {
  switch(action.type) {
    default:
      return state;
  }
}
